# msf

`msf` is a free and open-source, user-facing all-in-one management tool for DNS and proxy (mihomo / sing-box, in development).

The Unraid plugin stores persistent data under `/mnt/user/appdata/msf` by default and exposes a settings page under **Settings / MSF Free**.
